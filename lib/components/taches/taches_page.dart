import 'package:flutter/material.dart';

class TachesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tâches'),
      ),
      body: Center(
        child: Text('Page des tâches'),
      ),
    );
  }
}
